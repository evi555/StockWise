spring:
  application:
    name: Api-Gateway-service
  cloud:
    gateway:
      server:
        webflux:
          routes:
             - id: market-data-service
               uri: http://localhost:8081
               predicates:
                 - Path=/api/data/**
server:
  port: 8080
